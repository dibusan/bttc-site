!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "BTTC"
    %meta{content: "#{content_for?(:description) ? yield(:description) : "Rails Devise"}", name: "description"}/
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    / <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/malihu-custom-scrollbar-plugin/3.1.5/jquery.mCustomScrollbar.min.css">
    = csrf_meta_tags
  %body.container
    .wrapper
      = render 'layouts/navigation'
      .row.padded
        = link_to cart_path, class: 'col-sm-2' do
          %span.fa-stack.fa-2x.has-badge{"data-count" => "#{$redis.smembers(current_user_cart).count}"}
            %i.fa.fa-shopping-cart.fa-stack-1x
        .col-sm-10.padded.text-right
          = render 'layouts/social_media'
      .padded.sidebarCollapseWrapper
        %nav.navbar.navbar-expand-lg.navbar-light.bg-light
          .container-fluid
            / <button type="button" id="sidebarCollapse" class="btn btn-info">
            / <i class="fa fas fa-align-left"></i>
            / </button>
      #content.container.padded
        = render 'layouts/messages'
        = render 'layouts/navbar_collapse'
        = yield
    / <script src="https://cdnjs.cloudflare.com/ajax/libs/malihu-custom-scrollbar-plugin/3.1.5/jquery.mCustomScrollbar.concat.min.js"></script>